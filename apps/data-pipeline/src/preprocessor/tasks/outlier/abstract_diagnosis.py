"""
최하위 세부 태스크 레이어의 이상치 탐지 알고리즘들이 반드시 준수해야 하는 공통 계약 인터페이스 추상 클래스

[전체 데이터 흐름 설명 (Input -> Output)]
1. Input: 전방 마스터 오케스트레이터(OutlierDiagnosis)로부터 연산 계약 조건에 맞춰 위임받은 Pass 1 보간 완료 가격 데이터프레임.
2. Processing: 상속받은 하위 구체 클래스들이 각자의 수학적/통계적 알고리즘에 따라 이상치 좌표 마스킹 연산 수행.
3. Output: 원본 데이터와 물리적 차원 및 정렬축이 1:1 대칭을 이루는 순수 불리언 마스크 데이터프레임(pd.DataFrame).

주요 기능:
- [Interface Standardization] 다형성 바인딩 및 동적 조립을 위한 detect() 추상 메서드 명세 강제.
- [Side-Effect Defense] 입력 데이터프레임 원본의 상태 오염 및 메모리 침범을 방어하기 위한 추상화 계약 수립.

Trade-off: 주요 구현에 대한 엔지니어링 관점의 근거(장점, 단점, 근거) 요약.
- 추상 인터페이스 레이어 도입 vs 구체 클래스 직접 호출 및 분기 결합:
  - 장점: 새로운 이상치 탐지 알고리즘(예: Mahalanobis Distance, Autoencoder 등)이 추가되더라도 마스터 태스크인 OutlierDiagnosis 코드를 단 1줄도 수정하지 않는 극한의 개방 폐쇄 원칙(OCP)을 실현함.
  - 단점: 파일 본수가 늘어나고 상속 계층 토폴로지가 생기면서 전처리 서브 시스템의 파일 초기 복잡도가 소폭 증가함.
  - 근거: 금융 시계열 전처리는 다양한 통계 및 ML 모델의 교차 조합 실험 설계가 필수적임. 구체 클래스에 직접 의존하면 결합도가 치솟아 포트폴리오의 아키텍처 확장성이 파괴되므로, 추상 클래스를 통한 계약 강제가 절대적으로 타당함.
"""

from abc import ABC, abstractmethod
import pandas as pd


# ==============================================================================
# Abstract Class
# ==============================================================================
class AbstractOutlierDiagnosis(ABC):
    """하위 이상치 진단 구체 클래스들의 수리 연산 인터페이스 계약을 규정하는 추상 기저 클래스."""

    @abstractmethod
    def detect(self, df: pd.DataFrame) -> pd.DataFrame:
        """입력된 시계열 데이터프레임을 분석하여 이상치 좌표 위치를 명시한 불리언 마스크 데이터프레임을 생성합니다.

        하속 구체 클래스들은 이 메서드를 오버라이딩하여 자신만의 이상치 임계 스펙트럼을 연산해야 하며,
        반드시 원본 데이터의 행렬 차원과 인덱스 축을 변형 없이 보존하여 사출해야 합니다.

        Args:
            df (pd.DataFrame): 전방 레이어에서 1차 보간이 완료되어 NaN 잔차가 없는 
                조밀한(Dense) 상태의 금융 시계열 데이터프레임.

        Returns:
            pd.DataFrame: 원본 행렬과 차원이 1:1 일치하며, 이상치 위치는 True, 
                정상 위치는 False로 채워진 불리언 마스크 데이터프레임.
        """
        pass